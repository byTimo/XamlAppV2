using System.Net;

namespace ZappChat_v3.Core
{
    public static class Constants
    {
        public static readonly IPAddress ServerIp = IPAddress.Parse("localhost");
        public const int ServerPort = 14000;
    }
}