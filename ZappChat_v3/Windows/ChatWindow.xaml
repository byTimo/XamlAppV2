<Window x:Class="ZappChat_v3.Windows.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:ZappChat_v3.Controls"
        mc:Ignorable="d"
        Title="ChatWindow" WindowStyle="None" Background="{x:Null}" ResizeMode="CanResizeWithGrip" AllowsTransparency="True"
        MinHeight="600" MinWidth="800">
    <Border CornerRadius="5,5,0,5" Background="White" BorderThickness="1" BorderBrush="#FFCFCFCF">
        <Grid Margin="5,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="480*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="64"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <!--  -->
            <controls:StatusButton Grid.Row="0" Grid.Column="0" Content="Максим Гостюхин" FontFamily="HelveticaNeueCyr" FontSize="16" FontStretch="Condensed" FontWeight="Medium"/>
            <GridSplitter Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Background="White" ResizeBehavior="PreviousAndNext" Width="3"/>
            <TabControl x:Name="tabControl" Grid.Row="1" Grid.Column="0" Background="{x:Null}" BorderBrush="{x:Null}" Style="{DynamicResource TabControlChatMambers}">
                <TabItem Header="Друзья" Style="{DynamicResource TabItemChatMamber}">
                    <Grid Background="#FFE5E5E5"/>
                </TabItem>
                <TabItem Header="Группы" Style="{DynamicResource TabItemChatMamber}">
                    <Grid Background="#FFE5E5E5"/>
                </TabItem>
            </TabControl>
            <Button x:Name="button" Content="Х" Grid.Column="2" HorizontalAlignment="Right" Margin="0,18.667,10,21.333" Width="48.667" Click="button_Click"/>
        </Grid>
    </Border>
</Window>
